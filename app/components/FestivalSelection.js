import React from 'react';
import { SafeAreaView, View } from 'react-native';

const FestivalSelection = (props) => {
    return (
        <SafeAreaView>
            <View>
                <Text>Austin City Limits</Text>
            </View>
        </SafeAreaView>
    );
}

const styles = StyleSheet.create({
    container: {

    }
})

export default FestivalSelection;